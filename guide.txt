mysql -u root -p
GRANT ALL PRIVILEGES ON *.* TO 'test'@'%' WITH GRANT OPTION;

FLUSH PRIVILEGES;

CREATE DATABASE fastapi_blog;

use fastapi_blog;



alembic init migrations

alembic revision --autogenerate -m "Adding User and Post Table"
alembic upgrade head